#version 330 core

//interpolated values from the vertex shaders
in vec4 fragmentColor;

//ouput data
out vec4 color;

void main()
{
	//output color = color specified in the vertex shader, 
	//interpolated between all 4 surrounding vertices
	color = fragmentColor;
}